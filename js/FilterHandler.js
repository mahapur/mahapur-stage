function filter() {

  // We receive city & category both values when anyone of them changes (expected behaviour)

  // TODO - Filter table contents based on selected values
    const city = document.getElementById("city");
    const category = document.getElementById("category");

    console.log(city.value)
    console.log(category.value)

    tableBody = document.getElementById("table-id");
    return false;
}
